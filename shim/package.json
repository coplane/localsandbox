{
  "name": "bashfs-shim",
  "version": "0.1.0",
  "description": "TypeScript CLI shim for BashFS Python SDK",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "bashfs-shim": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --test"
  },
  "dependencies": {
    "agentfs-sdk": "^0.5.3",
    "just-bash": "^2.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
