[project]
name = "bashfs-py"
version = "0.1.0"
description = "A python SDK for a agentfs + just-bash agent sandbox."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ruff>=0.14.11",
]

[dependency-groups]
dev = [
    "pyright>=1.1.408",
    "pytest>=9.0.2",
]
